# The bundle identifier of your app
the_app_identifier = "org.reactjs.native.example.app"
app_identifier(the_app_identifier)

# Your Apple email address
# Uncomment and replace with your actual Apple ID when running fastlane commands
# apple_id("your_apple_id@example.com")

# The team identifier for your Apple Developer account
# You can find this in your Apple Developer account under Membership
# Uncomment and replace with your actual team ID
# team_id("YOUR_TEAM_ID")

# Load team ID from environment or use a default
# This should match the TEAM_ID in Fastfile
# ENV['FASTLANE_TEAM_ID'] ||= 'YOUR_TEAM_ID'  # Commented out to enable automatic team detection

# If you have multiple teams, you can specify which one to use
# team_id(ENV['FASTLANE_TEAM_ID'])

# Configure provisioning profiles
# You can use match for automatic provisioning profile management
# More info: https://docs.fastlane.tools/actions/match/
# Uncomment and configure if you're using match
# For development builds
# provisioning_profile_name("match Development #{the_app_identifier}")
# For app store builds
# for_platform :ios do
#   for_lane :build_release do
#     provisioning_profile_name("match AppStore #{the_app_identifier}")
#   end
# end

# For more information about the Appfile, see:
#     https://docs.fastlane.tools/advanced/#appfile
